<%= form_for(@attachment) do |f| %>
  <% if @attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attachment.errors.count, "error") %> prohibited this attachment from being saved:</h2>

      <ul>
      <% @attachment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :pitch_id %><br />
    <%= f.text_field :pitch_id, :value => @attachment_pitch_id, :readonly => true %>
  </div>
  <div class="field">
    <%= f.label :upload_url %><br />
    <%= f.text_field :upload_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
